<Window x:Class="SaveToGameWpf.Windows.AdbInstall"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d" Style="{StaticResource CommonWindowStyle}"
        MaxHeight="10000" MaxWidth="10000"
        Title="AdbInstall" Height="300" Width="300">
    <Grid Margin="5">
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Padding" Value="8, 3"/>
                <Setter Property="Margin" Value="0,0,5,0"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView></ListView>
        <StackPanel Grid.Row="1" Margin="0,5,0,0" Orientation="Horizontal">
            <Button Content="Обновить"/>
        </StackPanel>
    </Grid>
</Window>
